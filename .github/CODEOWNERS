# Only these accounts can approve changes to the site.
# The repo is public, so anyone may OPEN a pull request — GitHub offers no way
# to prevent that. This file, plus the "Protect production branches" ruleset,
# is what stops one being merged without an approval from a listed owner.
#
# Both owners are on one line, so an approval from EITHER satisfies the rule.
#
# NOTE: a listed account must have write access to be requestable as a
# reviewer. @fastaifoundry is not a collaborator yet, so GitHub will flag it
# as an unknown owner and it cannot approve until invited. Intentional for now.

* @UpendraBhandari @fastaifoundry
